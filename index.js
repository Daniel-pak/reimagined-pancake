// removing all code to establish a deep understanding of the index.js file

//import a library to help us build a component
import React from 'react';
import ReactNative from 'react-native';

//create a component
const App = () => {
  return (
    <Text>Some Text</Text> //looks a lot like html tags but it's jsx
  );
};

//render it to a device
